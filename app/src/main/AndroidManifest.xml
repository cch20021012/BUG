<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gug">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".zzx.LogisActivity"></activity>
        <activity android:name=".zzx.LeaveActivity" />
        <activity android:name=".zzx.HotActivity" />
        <activity android:name=".zzx.RankingActivity" />
        <activity android:name=".zzx.EventActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OneActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>